name: CI Light (Air-Gap)
on: [push, pull_request]
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Air-gap CI placeholder. Run locally for full tests."
      - name: Run AI light tests (best effort)
        run: >
          if command -v pnpm >/dev/null 2>&1; then
            pnpm run test:ai:light || echo "test:ai:light skipped (deps unavailable)";
          else
            echo "pnpm missing: skip AI light tests";
          fi
