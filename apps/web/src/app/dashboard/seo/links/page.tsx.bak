import { InternalLinkSuggestions } from '@/components/seo/InternalLinkSuggestions';

const placeholderContent = `Your customer data platform centralises first-party behavioural signals, but most nurture flows still rely on static rules.
To increase conversion velocity, connect product usage insights to dynamic playbooks that trigger personalised messaging.`;

export default function InternalLinksPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-2xl font-semibold tracking-tight">Internal Linking</h1>
        <p className="text-sm text-muted-foreground">
          Ensure every strategic asset is linked into your topic cluster. Paste a draft or recent article to receive recommendations.
        </p>
      </div>
      <InternalLinkSuggestions contentId="content-demo" initialContent={placeholderContent} />
    </div>
  );
}
