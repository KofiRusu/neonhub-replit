import { EventEmitter } from 'events';
import { AgentIntelligenceBus } from '../../../aib';
import { SelfHealingManager } from '../../../self-healing';
import { PredictiveEngine } from '../../../modules/predictive-engine/src/core/PredictiveEngine';
import { CognitiveInfrastructure } from '../../../cognitive-infra';
import { TestCase, TestExecution, BenchmarkResult, TelemetryData, FailurePattern, QASentinelConfig, CIDIntegration } from '../types';
export declare class QASentinel extends EventEmitter {
    private aib;
    private selfHealingManager;
    private predictiveEngine;
    private cognitiveInfra;
    private config;
    private logger;
    private isMonitoring;
    private testCases;
    private telemetryBuffer;
    private failurePatterns;
    private activeExecutions;
    constructor(aib: AgentIntelligenceBus, selfHealingManager: SelfHealingManager, predictiveEngine: PredictiveEngine, cognitiveInfra: CognitiveInfrastructure, config: QASentinelConfig);
    private setupEventHandlers;
    startMonitoring(): Promise<void>;
    stopMonitoring(): Promise<void>;
    private performMonitoringCycle;
    private collectTelemetry;
    private gatherSystemMetrics;
    private collectAPIMetrics;
    private collectDatabaseMetrics;
    private collectAgentMetrics;
    private collectInfrastructureMetrics;
    private analyzeFailurePatterns;
    private detectComponentAnomalies;
    private generateSuggestedTests;
    private generateIntelligentTests;
    private createMLGeneratedTest;
    private detectAnomalies;
    private triggerCriticalResponse;
    private updateTestExecutions;
    private evaluateSelfHealing;
    runBenchmark(buildId: string): Promise<BenchmarkResult>;
    private loadBaselineMetrics;
    private collectCurrentMetrics;
    private calculatePercentile;
    private compareMetrics;
    private analyzeBenchmarkAnomalies;
    integrateWithCI(pipelineId: string, stage: string): Promise<CIDIntegration>;
    private runCIValidation;
    private executeTest;
    private handleSystemMetrics;
    private handleSystemError;
    private handleAgentPerformance;
    private handleRepairCompleted;
    private handleRepairFailed;
    private handlePredictionAnomaly;
    getTestCases(): TestCase[];
    getFailurePatterns(): FailurePattern[];
    getActiveExecutions(): TestExecution[];
    getTelemetryBuffer(): TelemetryData[];
    isMonitoringActive(): boolean;
}
//# sourceMappingURL=QASentinel.d.ts.map